#include "main.h"

/**
 * mul - multiply two numbers
 * @a : parameter a
 * @b : parameter b
 * Return : return result;
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
