#include <stdio.h>
/**
 * main - body of function
 * Return: returns 0 when successful
 */
int main(void)
{
	char i;
	int counter;

	i = 'a';
	counter = 0;
	while (i <='z')
	{
		putchar(i);
		putchar('\n');

		counter++;
	}

	return (0);
}
