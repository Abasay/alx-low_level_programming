#include "main.h"

/**
 * _puts_recursion - print string with recursion
 * @s : string to print
 * Return: void
 */

void _puts_recursion(char *s)
{
	_putchar(*s);
	s++;
	if (*s != '\0')
		_puts_recursion(*s);
}
