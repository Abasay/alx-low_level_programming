#include <stdio.h>
/**
 * main - body of the function
 * Return: returns 0 if successful
 */
int main(void)
{
	int a;

	a = 48;
	while (a < 58)
	{
		putchar(a);
		a++;
	}
	putchar('\n');
	return (0);
}
