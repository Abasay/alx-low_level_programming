#include "main.h"
/**
 * main - check code
 *
 * Return: return per instruction
 */

int _isalpha(int c)
{
	if ((c >= 65 && c <= 90) && (c >= 97 && c <= 122)
		{
			return (1);
		}
		else
		{
			return (0);
		}
}
