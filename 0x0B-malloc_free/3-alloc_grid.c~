#include "main.h"

/**
 * alloc_grid - allocating grid
 * @width: width of array
 * @height: height of array
 * Return: int
 */

int **alloc_grid(int width, int height)
{
	unsigned int **size, i, j;

	if (width <= 0 || height <= 0)
		return (NULL);
	size = malloc(sizeof(int *) * height);

	if (size == NULL)
		return (NULL);
	for (i = 0; i < height; i++)
	{

		size[i] = malloc(sizeof( 3
