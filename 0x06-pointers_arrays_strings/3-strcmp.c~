#include "main.h"

/**
 * _strcmp - compare strings
 * @s1: string 1
 * @s2: string 2
 * Return: per length
 */

int _strcmp(char *s1, char *s2)
{
	char *i, *j;

	for (i = s1; i != '\0'; i++)
	{
	}
	for (j = s2; j != '\0'; j++)
	{
	}
	return (i - j);
}
